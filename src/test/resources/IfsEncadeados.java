class IfsEncadeados {
    public static void main(String[] args) {

        if (a > b) {
            // bloco de código 1
        } else if (b > c) {
            // bloco de código 2
        } else if (c > d) {
            // bloco de código 2
        } else {
            // bloco de código 3
        }

        int marks = 10;
        String result = (e > 40) ? "pass" : "fail";

        boolean x = (1 > 0) ? false : true;
    }

}